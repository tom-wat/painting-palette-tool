{
  "buildCommand": "pnpm turbo run build --scope=@palette-tool/web",
  "outputDirectory": "packages/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./packages/web"
}
